---
description: Choose the right Playground CLI command, mount strategy, and persistence model for a task
argument-hint: [path-or-goal]
---

Use this workflow to decide between `start`, `server`, `run-blueprint`, `build-snapshot`, and `runCLI()`.

Target: `$ARGUMENTS` or the current working directory.

Explain the recommended command, why it fits, and what version, mount, and persistence choices matter.

