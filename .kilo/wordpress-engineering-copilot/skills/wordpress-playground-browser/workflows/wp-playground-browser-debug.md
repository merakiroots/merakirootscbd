---
description: Browser-first debugging workflow for WordPress Playground sites, embeds, and iframe-based UI issues
argument-hint: [url-or-path]
---

Use this workflow when the task needs interactive browser validation for a Playground instance.

Target: `$ARGUMENTS` or the default local Playground URL.

Check iframe boundaries, console errors, network failures, and the exact WordPress page under test.

